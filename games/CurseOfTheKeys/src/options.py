
# if True, it prints a verbose battle log
VERBOSE=False

# if True, fonts are rendered to surface every time the gui updates
# instead of storing the surfaces in a dict in the gui class
NO_FONT_CACHE=False
